services:
  - type: web
    name: ascent-lab-web
    runtime: dotnet
    buildCommand: dotnet publish src/ascent-lab-web/ascent-lab-web.csproj -c Release -o out
    startCommand: cd out && dotnet ascent-lab-web.dll
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production