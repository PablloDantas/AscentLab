services:
  - type: web
    name: ascent-lab-web
    runtime: dotnet
    buildCommand: dotnet publish -c Release -o out
    startCommand: cd out && dotnet ascent-lab-web.dll
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production